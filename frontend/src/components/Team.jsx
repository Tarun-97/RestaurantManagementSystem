import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
            At Valtryek, our chefs aren’t just cooks—they’re culinary warriors. Each dish is forged with precision, passion, and a touch of mythic flair. From flame-seared feasts to delicate creations, our team blends tradition with innovation to serve experiences that linger long after the last bite.
            Together, they turn ingredients into legends.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
